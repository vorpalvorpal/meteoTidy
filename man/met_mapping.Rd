% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-mapping.R
\name{met_mapping}
\alias{met_mapping}
\title{Describe how to map a parsed response to canonical observations}
\usage{
met_mapping(format = c("json", "csv"), time, variables)
}
\arguments{
\item{format}{Single string, \code{"json"} or \code{"csv"}.}

\item{time}{A list describing how to find the timestamp:
\itemize{
\item for \code{format = "json"}: \code{list(path = "hourly/time", tz = "UTC")}, where
\code{path} is a \code{/}-separated walk through the parsed list.
\item for \code{format = "csv"}: \code{list(column = "timestamp", tz = "UTC")}.
\code{tz} is the timezone the raw timestamps are expressed in; they are
converted to UTC by \code{\link[=apply_mapping]{apply_mapping()}}.
}}

\item{variables}{A list, one entry per source field, each a list with:
\itemize{
\item \code{variable} — the canonical dictionary variable name.
\item \code{path} (JSON) or \code{column} (CSV) — where to find the raw values.
\item \code{unit} — the \strong{source} unit (a \code{units}-package-parseable string);
\code{\link[=apply_mapping]{apply_mapping()}} calls \code{to_canonical()} to convert it.
\item \code{height} — (optional) a \code{units}-classed double (metres), the sensor
height this field was measured at, recorded for later height
correction (Plan 11); not used by \code{apply_mapping()} itself.
}}
}
\value{
A \code{met_mapping} object (a validated list with class
\code{"meteoTidy::met_mapping"}).
}
\description{
A declarative spec describing how to turn an already-parsed source
response (a JSON list, or a CSV data frame) into canonical long-format
observation rows. Built from YAML (Plan 02 site config) or written by hand
(as in \code{source_rest()}/\code{source_file()} examples).
}
\examples{
met_mapping(
  format = "json",
  time = list(path = "hourly/time", tz = "UTC"),
  variables = list(
    list(variable = "temperature_2m", path = "hourly/temperature_2m",
         unit = "degC", height = units::set_units(2, "m"))
  )
)
}
\seealso{
Other adapter: 
\code{\link{adapters_for_site}()},
\code{\link{apply_mapping}()},
\code{\link{fetch}()},
\code{\link{fetch_forecast}()},
\code{\link{met_adapter}()},
\code{\link{met_attribution}()},
\code{\link{resolve_station}()},
\code{\link{source_file}()},
\code{\link{source_openmeteo}()},
\code{\link{source_rest}()}
}
\concept{adapter}
