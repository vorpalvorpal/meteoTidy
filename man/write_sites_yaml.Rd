% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site-yaml.R
\name{write_sites_yaml}
\alias{write_sites_yaml}
\title{Write a site registry to YAML}
\usage{
write_sites_yaml(sites, path, include_resolved = FALSE)
}
\arguments{
\item{sites}{A \code{\link[=met_sites]{met_sites()}} or a single \code{\link[=met_site]{met_site()}}.}

\item{path}{Single string, output file path.}

\item{include_resolved}{Logical, default \code{FALSE}. If \code{TRUE}, snapshot the
\code{resolved} cache into the written file (useful for debugging only; the
version-controlled config should not normally carry it).}
}
\value{
\code{path}, invisibly.
}
\description{
The inverse of \code{\link[=read_sites_yaml]{read_sites_yaml()}}: serialises a \code{\link[=met_sites]{met_sites()}} (or a
single \code{\link[=met_site]{met_site()}}) back to the documented YAML schema. \code{units} are
stripped back to plain numbers in the documented canonical unit, so
\code{read_sites_yaml(path) |> write_sites_yaml(tmp) |> read_sites_yaml()}
round-trips to an equivalent \code{met_sites}. The \code{resolved} external-ID cache
is a runtime cache, not configuration, so it is excluded by default.
}
\examples{
site <- met_site(
  site_id = "example",
  latitude = units::set_units(-34.75, "degree"),
  longitude = units::set_units(148.20, "degree"),
  elevation = units::set_units(220, "m"),
  timezone = "Australia/Sydney",
  instruments = list(),
  sources = list(),
  store_root = tempfile()
)
tmp <- tempfile(fileext = ".yaml")
write_sites_yaml(met_sites(list(site)), tmp)
}
\seealso{
Other site: 
\code{\link{as_met_sites}()},
\code{\link{met_instrument}()},
\code{\link{met_site}()},
\code{\link{met_sites}()},
\code{\link{read_sites_yaml}()},
\code{\link{site_id}()},
\code{\link{site_ids}()},
\code{\link{site_resolved}()}
}
\concept{site}
