% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source-ecmwf.R
\name{source_ecmwf}
\alias{source_ecmwf}
\title{An ECMWF Open Data acquisition adapter}
\usage{
source_ecmwf(stream = "enfo", resolution = "0p25", source_id = "ecmwf")
}
\arguments{
\item{stream}{Single string, the ECMWF Open Data stream identifier.
Default \code{"enfo"} (the real, open medium-range ensemble).}

\item{resolution}{Single string, the grid resolution identifier used to
build request URLs. Default \code{"0p25"}.}

\item{source_id}{Single string stamped into the \code{source} column of every
returned row. Default \code{"ecmwf"}.}
}
\value{
A \code{source_ecmwf} (\code{met_adapter} subclass) S7 object.
}
\description{
\code{source_ecmwf()} builds a \code{\link[=met_adapter]{met_adapter()}} that fetches ECMWF's Open Data
ensemble forecast as GRIB2, read via \strong{eccodes} (ECMWF's own GRIB library;
\code{R/grib-read.R}). eccodes is a \strong{hard requirement} for this adapter: every
fetch path checks for it first (see the internal \code{.have_eccodes()}) and
aborts a guided \code{"eccodes_required"} error -- pointing at
\code{\link[=ecmwf_install_eccodes]{ecmwf_install_eccodes()}} to provision it, or
\code{source_openmeteo(product = "seasonal")} as the no-GRIB degradation path
(SCOPING §5.2) -- if it is unavailable.
}
\details{
\code{stream} defaults to \code{"enfo"}, the real medium-range ensemble (~360h/15-day
horizon, 101 -> 50 perturbed members \verb{1..50}, no separate control member in
the open feed) confirmed present in ECMWF's live open-data catalogue as of
2026-07-06. The plan's original target, \code{"eefo"} (a hypothetical 46-day
extended-range stream), is \strong{not currently open}; it remains an accepted
value (so this adapter picks it up automatically if ECMWF ships it) but
will presently fail with a real HTTP 404 from \code{fetch_forecast()}. There is
therefore no long-range (46-day) coverage via \code{source_ecmwf()} today --
\code{source_openmeteo(product = "seasonal")} is the only long-range channel
(see \code{plans/08-acquisition-ecmwf.md}'s deviation note for the full record).
\code{resolution} defaults to \code{"0p25"} (0.25 degrees) but is kept as a
parameter, not hard-coded, since ECMWF has flagged a possible future move
to 0.125 degrees.
}
\examples{
adapter <- source_ecmwf()
}
\seealso{
Other adapter: 
\code{\link{adapters_for_site}()},
\code{\link{apply_mapping}()},
\code{\link{ecmwf_install_eccodes}()},
\code{\link{fetch}()},
\code{\link{fetch_forecast}()},
\code{\link{fetch_forecast_aux}()},
\code{\link{met_adapter}()},
\code{\link{met_attribution}()},
\code{\link{met_mapping}()},
\code{\link{resolve_station}()},
\code{\link{source_bom_forecast}()},
\code{\link{source_bom_obs}()},
\code{\link{source_file}()},
\code{\link{source_ghcnh}()},
\code{\link{source_openmeteo}()},
\code{\link{source_rest}()},
\code{\link{source_silo}()},
\code{\link{station_coverage}()}
}
\concept{adapter}
