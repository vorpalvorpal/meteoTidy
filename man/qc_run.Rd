% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{qc_run}
\alias{qc_run}
\title{Run the QC engine over a site's observation window}
\usage{
qc_run(
  store_root,
  site,
  variables = NULL,
  now = .now(),
  donors = NULL,
  history_daily = NULL
)
}
\arguments{
\item{store_root}{Root directory of the store.}

\item{site}{A \code{met_site} object.}

\item{variables}{Character vector of variables to QC; \code{NULL} (default)
means every variable present in the window.}

\item{now}{Injectable current time; see \code{.now()}.}

\item{donors}{A named list keyed by variable name, each element a list of
single-variable long donor series tibbles (see \code{qc_spatial()}). \code{NULL}
(default) means no donors are available, so the spatial rule logs
"insufficient donors" for every variable rather than erroring.}

\item{history_daily}{A climatology tibble for the climatological-bounds
rule (see \code{qc_climatology()}), or \code{NULL} (default) if none exists yet.}
}
\value{
Invisibly, a list \verb{(n_flagged, n_log_rows)} summarising the run.
}
\description{
Reads the QC watermark for \code{site} (a store watermark keyed by \code{source = "qc"}), applies a small look-back before it (\code{.qc_lookback()}) so a
late-arriving donor observation can retrigger the spatial rule on the
recent tail, reads the resulting window's observations, dispatches every
variable present to the rules \code{qc_rules_for_variable()} says apply
(combining flags worst-wins across rules), writes back via
\code{store_write_obs(mode = "supersede")} only the rows whose flag actually
changed, appends an auditable record to \code{qc_log}, and advances the
watermark to \code{now}.
}
\details{
Incremental: only the window from \code{watermark - lookback} to \code{now} is
scanned. Idempotent: re-running over the same window reproduces identical
flags and does not create duplicate \code{qc_log} rows (the log is
deduplicated on \verb{(site_id, datetime_utc, variable, rule)}, keeping the
latest run's verdict; see \code{qc_log_read()}).
}
\examples{
\dontrun{
root <- withr::local_tempdir()
site <- met_site(
  site_id = "example",
  latitude = units::set_units(-34.75, "degree"),
  longitude = units::set_units(148.20, "degree"),
  elevation = units::set_units(220, "m"),
  timezone = "Australia/Sydney",
  instruments = list(),
  sources = list(),
  store_root = root
)
qc_run(root, site)
}
}
\concept{qc}
