% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met-backfill.R
\name{met_backfill}
\alias{met_backfill}
\title{Day-0 bootstrap: full history, AWS export ingestion, initial fits, and a
donor-coverage audit}
\usage{
met_backfill(sites, now = .now(), config, aws_export = NULL)
}
\arguments{
\item{sites}{A \code{met_site} or \code{met_sites} collection.}

\item{now}{Injectable current time; see \code{.now()}.}

\item{config}{A pipeline configuration list (see \verb{plans/14-*}/
\code{tests/testthat/helper-pipeline.R}'s \code{pipeline_config()}): at least
\code{store_root}, \code{obs_sources}, \code{forecast_sources}.}

\item{aws_export}{Optional single string, a path/glob to a historical AWS
logger CSV export to ingest via \code{ingest_aws_export()}. \code{NULL} (default)
skips AWS ingestion.}
}
\value{
A tibble with one row per site: \code{site_id}, \code{coverage} (a
list-column, each element the \code{station_coverage()}-shaped donor audit
tibble for that site), \code{forecast_gaps} (a list-column, each element the
\code{archive_forecasts(missed = TRUE)} gap-backfillability summary for that
site).
}
\description{
Ad hoc (SCOPING section 7.2/9). Per site: pulls full historical
observations for each configured obs source (SILO/ERA5/Open-Meteo
Previous-Runs, whatever \code{config$obs_sources} configures) via
\code{.acquire_obs()}; ingests a historical AWS logger export when
\code{aws_export} is supplied (\code{ingest_aws_export()}); makes initial
calibration fits (\code{correct_refit()}); and runs the \strong{per-site
donor-coverage audit} (\code{station_coverage()}, Plan 06) so the operator
sees coverage gaps (e.g. a variable with no nearby GHCNh donor) before
relying on the gap-fill donor ladder (SCOPING section 13).
}
\details{
Also documents forecast-gap backfillability for each configured forecast
source (\code{archive_forecasts(..., missed = TRUE)}): BOM forecast gaps
cannot be backfilled; Open-Meteo gaps self-heal via Previous/Single Runs.
}
\examples{
\dontrun{
met_backfill(site, config = my_pipeline_config, aws_export = "logger-a.csv")
}
}
\seealso{
Other pipeline: 
\code{\link{met_refit}()},
\code{\link{met_sync_daily}()},
\code{\link{met_sync_live}()}
}
\concept{pipeline}
