% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source-rest.R
\name{source_rest}
\alias{source_rest}
\title{A generic REST-API observation adapter}
\usage{
source_rest(
  source_id,
  endpoint,
  mapping,
  auth = c("none", "header", "basic"),
  token_env = NULL,
  provides = NULL,
  cadence = "hourly"
)
}
\arguments{
\item{source_id}{Single string, stamped into the \code{source} column of every
returned row.}

\item{endpoint}{Single string, a URL template. Supported placeholders:
\itemize{
\item \code{{site}} — replaced with \code{site_id(site)}.
\item \code{{from}}, \code{{to}} — replaced with the requested window bounds,
formatted \code{"\%Y-\%m-\%dT\%H:\%M:\%SZ"} (an ISO instant; substring-matches a
bare \code{"\%Y-\%m-\%d"} date prefix too, since \code{format()} renders the date
first).
}}

\item{mapping}{A \code{\link[=met_mapping]{met_mapping()}} describing how to turn the parsed JSON
body into canonical rows.}

\item{auth}{One of \code{"none"} (default), \code{"header"}, or \code{"basic"}.
\itemize{
\item \code{"header"} sends \verb{Authorization: <token>}, where \verb{<token>} is read from
the environment variable named by \code{token_env} \strong{at fetch time}; the
token is never stored on the adapter object and never appears in
\code{print()}/\code{format()} output (SCOPING §11).
\item \code{"basic"} uses \code{httr2::req_auth_basic()} with the username and
password read from \code{paste0(token_env, "_USER")} and
\code{paste0(token_env, "_PASS")} at fetch time.
}}

\item{token_env}{Single string, the \emph{name} of the environment variable
holding the secret (never the secret itself). Required when
\code{auth != "none"}.}

\item{provides}{Character vector of variables this adapter can return.
Defaults to the variable names declared in \code{mapping}.}

\item{cadence}{Single string, a scheduling hint (default \code{"hourly"}).}
}
\value{
A \code{source_rest} (\code{met_adapter} subclass) S7 object.
}
\description{
\code{source_rest()} builds a \code{\link[=met_adapter]{met_adapter()}} that fetches a single-page JSON
response from a REST endpoint and maps it to canonical observations via
\code{\link[=apply_mapping]{apply_mapping()}}. Intended for simple automatic-weather-station APIs that
return one page of hourly data per request; anything needing pagination,
OAuth, or multi-page assembly is out of scope (SCOPING §13) — write a
bespoke adapter instead.
}
\examples{
adapter <- source_rest(
  "site_aws", "https://aws.example/api?site={site}&from={from}&to={to}",
  met_mapping(
    format = "json",
    time = list(path = "hourly/time", tz = "UTC"),
    variables = list(
      list(variable = "temperature_2m", path = "hourly/temperature_2m",
           unit = "degC")
    )
  )
)
}
\seealso{
Other adapter: 
\code{\link{adapters_for_site}()},
\code{\link{apply_mapping}()},
\code{\link{fetch}()},
\code{\link{fetch_forecast}()},
\code{\link{met_adapter}()},
\code{\link{met_attribution}()},
\code{\link{met_mapping}()},
\code{\link{resolve_station}()},
\code{\link{source_file}()},
\code{\link{source_ghcnh}()},
\code{\link{source_openmeteo}()},
\code{\link{source_silo}()},
\code{\link{station_coverage}()}
}
\concept{adapter}
