% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{abort_meteo}
\alias{abort_meteo}
\title{Signal a classed meteoTidy error}
\usage{
abort_meteo(
  message,
  ...,
  class,
  call = rlang::caller_env(),
  .envir = parent.frame()
)
}
\arguments{
\item{message}{A \code{cli}-formatted character vector (supports inline markup
such as \code{{.val }} and named bullets \code{"i"}, \code{"x"}, \code{"!"}).}

\item{...}{Passed on to \code{\link[cli:cli_abort]{cli::cli_abort()}}.}

\item{class}{Required. The short condition class, e.g. \code{"bad_units"}.}

\item{call}{The call to report as the origin of the error. Defaults to the
caller of \code{abort_meteo()}, not \code{abort_meteo()} itself.}

\item{.envir}{Environment used for glue interpolation of \code{message}.}
}
\value{
Never returns; always signals an error.
}
\description{
Wraps \code{\link[cli:cli_abort]{cli::cli_abort()}}, requiring a \code{class} so every error raised by the
package can be caught precisely. The short \code{class} supplied is prefixed
with \code{"meteoTidy_error_"}; the umbrella class \code{"meteoTidy_error"} is always
attached as well.
}
\examples{
f <- function() abort_meteo("Something went wrong.", class = "demo")
tryCatch(f(), meteoTidy_error_demo = function(cnd) cnd$message)
}
\seealso{
Other conditions: 
\code{\link{inform_meteo}()},
\code{\link{meteo_conditions}()},
\code{\link{warn_meteo}()}
}
\concept{conditions}
