% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source-silo.R
\name{source_silo}
\alias{source_silo}
\title{A SILO acquisition adapter (weatherOz)}
\usage{
source_silo(
  api_key_env,
  dataset = c("patched_point", "data_drill"),
  source_id = "silo"
)
}
\arguments{
\item{api_key_env}{Single string, the name of the environment variable
holding the SILO email address.}

\item{dataset}{One of \code{"patched_point"} (station-based) or \code{"data_drill"}
(gridded).}

\item{source_id}{Single string stamped into the \code{source} column. Default
\code{"silo"}.}
}
\value{
A \code{source_silo} (\code{met_adapter} subclass) S7 object.
}
\description{
\code{source_silo()} builds a \code{\link[=met_adapter]{met_adapter()}} that fetches daily Australian
climate series from SILO via \code{weatherOz}, either PatchedPoint (nearest BOM
station, patched/interpolated) or DataDrill (5x5 km gridded surface).
}
\details{
The SILO "API key" is an email address (PII, not a secret — SCOPING §11):
it is read from the named environment variable \strong{at fetch/resolve time
only}, never stored on the adapter object, and never written into any
returned data column.

Each SILO daily value carries a source/quality code (see
\code{\link[=silo_qcode_map]{silo_qcode_map()}}) that is mapped into the canonical \code{method}/\code{qc_flag}
columns rather than being collapsed to a blanket \code{"measured"}/\code{"ok"}
(SCOPING §5: "ingest SILO source/quality codes into provenance").

SILO's daily boundary is the \strong{9am local-clock-time} rainfall-day
convention (SCOPING §3): each row's \code{date} is mapped to 9am local clock
time in the site's IANA timezone, expressed as the equivalent UTC instant
(DST-aware — the same wall-clock 9am is a different UTC hour in summer vs
winter).
}
\examples{
adapter <- source_silo(api_key_env = "SILO_EMAIL", dataset = "patched_point")
}
\seealso{
Other adapter: 
\code{\link{adapters_for_site}()},
\code{\link{apply_mapping}()},
\code{\link{ecmwf_install_eccodes}()},
\code{\link{fetch}()},
\code{\link{fetch_forecast}()},
\code{\link{fetch_forecast_aux}()},
\code{\link{met_adapter}()},
\code{\link{met_attribution}()},
\code{\link{met_mapping}()},
\code{\link{resolve_station}()},
\code{\link{source_bom_forecast}()},
\code{\link{source_bom_obs}()},
\code{\link{source_ecmwf}()},
\code{\link{source_file}()},
\code{\link{source_ghcnh}()},
\code{\link{source_openmeteo}()},
\code{\link{source_rest}()},
\code{\link{station_coverage}()}
}
\concept{adapter}
