% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/silo-qcode.R
\name{silo_qcode_map}
\alias{silo_qcode_map}
\title{Map a SILO source/quality code to (method, qc_flag)}
\usage{
silo_qcode_map(code)
}
\arguments{
\item{code}{Single string, the SILO source/quality code (as delivered by
weatherOz, e.g. \code{"0"}, \code{"25"}, \code{"35"}, \code{"75"}).}
}
\value{
A list with \code{method} and \code{qc_flag}, both single strings.
}
\description{
Looks \code{code} up in \code{\link[=silo_qcode_reference]{silo_qcode_reference()}}. Observed codes map to
\code{method = "measured"}, \code{qc_flag = "ok"}; interpolated/patched codes map to
\code{method} in \code{c("imputed", "model_fill")} (grid-interpolated DataDrill
values use \code{"model_fill"}; station-neighbourhood interpolation uses
\code{"imputed"}), \code{qc_flag = "ok"}; the long-term-average fallback code maps
to \code{qc_flag = "suspect"} (SCOPING §5: "ingest SILO source/quality codes
into provenance"). An unrecognised code aborts class \code{"unknown_silo_code"}
rather than silently defaulting, so a SILO schema change is caught instead
of mis-provenanced.
}
\examples{
silo_qcode_map("0")
silo_qcode_map("35")
}
\seealso{
Other silo: 
\code{\link{silo_qcode_reference}()}
}
\concept{silo}
