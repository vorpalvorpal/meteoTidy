% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site.R
\name{met_site}
\alias{met_site}
\title{A meteorological site}
\usage{
met_site(
  site_id = character(0),
  latitude = numeric(0),
  longitude = numeric(0),
  elevation = numeric(0),
  timezone = character(0),
  instruments = list(),
  sources = list(),
  store_root = character(0),
  resolved = .empty_resolved()
)
}
\arguments{
\item{site_id}{Single string, non-empty, matching \verb{[A-Za-z0-9_-]+}. The
join key used everywhere in the canonical store.}

\item{latitude, longitude}{A \code{units}-classed double (degrees). Latitude in
\verb{[-90, 90]}, longitude in \verb{[-180, 180]}.}

\item{elevation}{A \code{units}-classed double (metres), station elevation.}

\item{timezone}{Single string, an IANA timezone name (validated against
\code{\link[=OlsonNames]{OlsonNames()}}).}

\item{instruments}{A list of \code{\link[=met_instrument]{met_instrument()}} objects.}

\item{sources}{A named list of raw source configs (opaque here; parsed by
adapters in Plan 04).}

\item{store_root}{Single string, the filesystem path (or URI) for this
site's Parquet tree.}

\item{resolved}{A list, the external-ID resolution cache. See
\code{\link[=site_resolved]{site_resolved()}} / \code{\link[=site_set_resolved]{site_set_resolved()}}. Defaults to an empty
\code{bom}/\code{ghcnh}/\code{silo} skeleton.}
}
\value{
A \code{met_site} S7 object.
}
\description{
An S7 class describing a single monitoring site: location, timezone,
instrumentation, source configuration, storage location, and a cache of
external station/grid identifiers resolved by adapters.
}
\examples{
met_site(
  site_id = "example",
  latitude = units::set_units(-34.75, "degree"),
  longitude = units::set_units(148.20, "degree"),
  elevation = units::set_units(220, "m"),
  timezone = "Australia/Sydney",
  instruments = list(),
  sources = list(),
  store_root = tempfile()
)
}
\seealso{
Other site: 
\code{\link{as_met_sites}()},
\code{\link{met_instrument}()},
\code{\link{met_sites}()},
\code{\link{read_sites_yaml}()},
\code{\link{site_id}()},
\code{\link{site_ids}()},
\code{\link{site_resolved}()},
\code{\link{write_sites_yaml}()}
}
\concept{site}
