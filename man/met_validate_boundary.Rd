% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met-table-hash.R
\name{met_validate_boundary}
\alias{met_validate_boundary}
\title{Validate a \code{met_table} at a consumer boundary (the hash re-check)}
\usage{
met_validate_boundary(x)
}
\arguments{
\item{x}{A \code{met_table}.}
}
\value{
The \code{met_table}, with \code{met_provenance(x)$tier} updated for any
column whose content hash no longer matches.
}
\description{
Recomputes each value column's content hash from its \emph{current} data and
compares it against the hash stored at construction time
(\code{attr(x, "content_hash")}). Any column whose hash now differs was
mutated in place since the metadata was set (something
\code{dplyr_reconstruct()} cannot see) -- that column's provenance \code{tier} is
downgraded to \code{"unverified"}, leaving every other column's provenance
untouched. This is a per-column, granular downgrade, distinct from the
whole-object downgrade-to-plain-tibble path in \code{R/met-table-dplyr.R}.
}
\examples{
mt <- new_met_table(
  tibble::tibble(time = as.POSIXct("2026-01-01", tz = "UTC"), temperature_2m = 20),
  provenance = tibble::tibble(variable = "temperature_2m", tier = "raw",
                             train_overlap = 0, source = "openmeteo"),
  keys = list(site_id = "test"),
  versions = list(schema_version = "1.0.0", calibration_manifest_version = 0L)
)
met_validate_boundary(mt)
}
\seealso{
Other met-table: 
\code{\link{met_assert_single_tier}()},
\code{\link{met_ingest}()},
\code{\link{met_provenance}()},
\code{\link{met_wide}()},
\code{\link{new_met_table}()}
}
\concept{met-table}
