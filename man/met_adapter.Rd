% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter.R
\name{met_adapter}
\alias{met_adapter}
\title{The meteoTidy adapter contract}
\usage{
met_adapter(source_id = character(0), provides = character(0), cadence = NULL)
}
\arguments{
\item{source_id}{Single string, the \code{source} value stamped into every row
returned by this adapter's \code{\link[=fetch]{fetch()}} (e.g. \code{"site_aws"}, \code{"silo"},
\code{"openmeteo"}).}

\item{provides}{Character vector of dictionary variable names this adapter
can return.}

\item{cadence}{A documentation/scheduling hint, not enforced. Usually a
single string, one of \code{"hourly"}, \code{"daily"}, \code{"subdaily"}, \code{"per_issue"}.
An adapter may instead give a structured list when a simple label isn't
enough — e.g. \code{source_ghcnh()} (Plan 06) sets \code{cadence = list(live = FALSE, lag_days = 7)} so the pipeline (Plan 16) can programmatically tell
a best-effort backfill source apart from a live-head one.}
}
\value{
A \code{met_adapter} S7 object. In practice, users construct a subclass
(\code{\link[=source_rest]{source_rest()}}, \code{\link[=source_file]{source_file()}}) rather than \code{met_adapter()} directly.
}
\description{
\code{met_adapter} is the S7 base class every acquisition adapter extends
(\code{source_rest()}, \code{source_file()}, and provider-specific adapters added by
later plans). It carries only the properties shared by every adapter; the
actual fetch behaviour is implemented by \code{\link[=fetch]{fetch()}} methods on subclasses.
}
\examples{
met_adapter(source_id = "example", provides = "temperature_2m")
}
\seealso{
Other adapter: 
\code{\link{adapters_for_site}()},
\code{\link{apply_mapping}()},
\code{\link{ecmwf_install_eccodes}()},
\code{\link{fetch}()},
\code{\link{fetch_forecast}()},
\code{\link{fetch_forecast_aux}()},
\code{\link{met_attribution}()},
\code{\link{met_mapping}()},
\code{\link{resolve_station}()},
\code{\link{source_bom_forecast}()},
\code{\link{source_bom_obs}()},
\code{\link{source_ecmwf}()},
\code{\link{source_file}()},
\code{\link{source_ghcnh}()},
\code{\link{source_openmeteo}()},
\code{\link{source_rest}()},
\code{\link{source_silo}()},
\code{\link{station_coverage}()}
}
\concept{adapter}
