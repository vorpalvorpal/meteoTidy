% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source-openmeteo.R
\name{source_openmeteo}
\alias{source_openmeteo}
\title{An Open-Meteo acquisition adapter}
\usage{
source_openmeteo(
  product = c("forecast", "ensemble", "historical", "historical_forecast",
    "previous_runs", "single_runs", "seasonal"),
  models = NULL,
  api_key_env = NULL,
  source_id = "openmeteo",
  ...
)
}
\arguments{
\item{product}{One of \code{"forecast"}, \code{"ensemble"}, \code{"historical"},
\code{"historical_forecast"}, \code{"previous_runs"}, \code{"single_runs"},
\code{"seasonal"}. Selects the endpoint and the response shape.}

\item{models}{Optional character vector of underlying NWP model ids (see
the (non-exhaustive, extensible) roster in \code{R/openmeteo-endpoints.R}).
\code{NULL} (default) lets Open-Meteo pick its default model(s) for the
product.}

\item{api_key_env}{Optional single string: the \emph{name} of an environment
variable holding a commercial Open-Meteo API key. See Licensing above.}

\item{source_id}{Single string stamped into the \code{source} column of every
returned row. Default \code{"openmeteo"}.}

\item{...}{Reserved for future product-specific options.}
}
\value{
A \code{source_openmeteo} (\code{met_adapter} subclass) S7 object.
}
\description{
\code{source_openmeteo()} builds a \code{\link[=met_adapter]{met_adapter()}} that fetches from one of
Open-Meteo's products: Forecast, Ensemble, Historical Weather (ERA5),
Historical Forecast, Previous Runs, Single Runs, and Seasonal.
}
\details{
\subsection{Licensing (SCOPING §10)}{

The free tier is licensed for \strong{non-commercial use only}, at up to 10 000
calls/day, with no API key required. \strong{No product in this adapter aborts
for lack of a key} -- the free tier technically serves every product
wrapped here, including Historical Weather and Ensemble. When
\code{api_key_env} is unset, \code{fetch()}/\code{fetch_forecast()} target the free host
and emit a one-time \code{\link[=inform_meteo]{inform_meteo()}} reminder that the free tier is
non-commercial only.

Commercial deployments need a \strong{paid} Open-Meteo plan, and within those
paid plans, the Historical/Climate/Ensemble/Satellite-Radiation APIs
additionally require the \strong{Professional tier or above}. That is a
commercial-plan boundary, not a technical key gate this adapter enforces:
set \code{api_key_env} to the name of an environment variable holding a
commercial key and the adapter targets the \verb{customer-} API host and sends
the key; which paid plan is required for a given product/volume is the
caller's responsibility to arrange with Open-Meteo.

The key is read from the named environment variable \strong{at fetch time only}
-- it is never stored on the adapter object, never appears in
\code{print()}/\code{format()} output, and never appears in any column of a returned
tibble.
}
}
\examples{
adapter <- source_openmeteo(product = "forecast")
adapter2 <- source_openmeteo(product = "historical", api_key_env = "OPEN_METEO_KEY")
}
\seealso{
Other adapter: 
\code{\link{adapters_for_site}()},
\code{\link{apply_mapping}()},
\code{\link{fetch}()},
\code{\link{fetch_forecast}()},
\code{\link{met_adapter}()},
\code{\link{met_attribution}()},
\code{\link{met_mapping}()},
\code{\link{resolve_station}()},
\code{\link{source_file}()},
\code{\link{source_rest}()}
}
\concept{adapter}
