% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met-refit.R
\name{met_refit}
\alias{met_refit}
\title{Refit correction calibrations and run verification (monthly)}
\usage{
met_refit(sites, now = .now(), config)
}
\arguments{
\item{sites}{A \code{met_site} or \code{met_sites} collection.}

\item{now}{Injectable current time; see \code{.now()}.}

\item{config}{A pipeline configuration list (see \verb{plans/14-*}/
\code{tests/testthat/helper-pipeline.R}'s \code{pipeline_config()}): at least
\code{store_root}, \code{obs_sources}, \code{forecast_sources}.}
}
\value{
A tibble with columns \code{site_id}, \code{status}, \code{message}.
}
\description{
Per site (SCOPING section 9): calls \code{correct_refit()} once per configured
source (\code{config$obs_sources} union \code{config$forecast_sources}, Plan 17
item 3) to fit/refit candidate calibration tiers and bump the calibration
manifest \strong{only} when Plan 13's skill gate passes (\code{correct_refit()}
itself performs the verdict-gated \code{calib_write()}); runs \code{verify_run()} over
\code{config$forecast_sources} for the verification report; and compacts the
store's Parquet partitions (\code{store_compact()}, Plan 03).
}
\details{
Idempotent: a second call over the same inputs and clock re-verifies but
only re-writes a calibration if the skill gate passes again; compaction
never changes which rows are readable, only the number of underlying
part-files.
}
\examples{
\dontrun{
met_refit(site, config = my_pipeline_config)
}
}
\seealso{
Other pipeline: 
\code{\link{met_backfill}()},
\code{\link{met_sync_daily}()},
\code{\link{met_sync_live}()}
}
\concept{pipeline}
