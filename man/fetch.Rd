% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter.R
\name{fetch}
\alias{fetch}
\title{Fetch canonical observations from an adapter}
\usage{
fetch(adapter, site, variables, window, now = .now())
}
\arguments{
\item{adapter}{A \code{\link[=met_adapter]{met_adapter()}} subclass instance.}

\item{site}{A \code{met_site} object (see Plan 02).}

\item{variables}{Character vector of dictionary variable names requested.}

\item{window}{A list with \code{from}/\code{to}, both UTC \code{POSIXct} scalars.}

\item{now}{Injectable clock; defaults to \code{.now()}.}
}
\value{
A canonical long observation tibble.
}
\description{
The core of the adapter contract (SCOPING §5): every adapter implements a
\code{fetch()} method returning a canonical long observation tibble (see the
internal \code{new_obs()}) for the requested \code{variables} over \code{window}.
}
\details{
Implementations must:
\enumerate{
\item Request/parse only the requested \code{variables} (intersected with
\code{provides}).
\item Convert every value to canonical units via \code{to_canonical()} \strong{before}
returning (SCOPING §3.1 unit pinning).
\item Stamp \code{source = source_id}, an appropriate \code{method}, and
\code{qc_flag = "ok"} (QC happens later; the adapter asserts nothing about
quality beyond "as delivered").
\item Return an empty (0-row) canonical table, not \code{NULL}, when the source has
no data for \code{window}.
}
}
\seealso{
Other adapter: 
\code{\link{adapters_for_site}()},
\code{\link{apply_mapping}()},
\code{\link{fetch_forecast}()},
\code{\link{met_adapter}()},
\code{\link{met_mapping}()},
\code{\link{resolve_station}()},
\code{\link{source_file}()},
\code{\link{source_rest}()}
}
\concept{adapter}
