% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met-sync-live.R
\name{met_sync_live}
\alias{met_sync_live}
\title{Sync the live (near-real-time) observation and forecast head}
\usage{
met_sync_live(sites, now = .now(), config)
}
\arguments{
\item{sites}{A \code{met_site} or \code{met_sites} collection.}

\item{now}{Injectable current time; see \code{.now()}.}

\item{config}{A pipeline configuration list (see \verb{plans/14-*}/
\code{tests/testthat/helper-pipeline.R}'s \code{pipeline_config()}): at least
\code{store_root}, \code{obs_sources}, \code{forecast_sources}.}
}
\value{
A tibble with columns \code{site_id}, \code{status} (\code{"ok"} or
\code{"degraded"}/\code{"error"}), \code{message}.
}
\description{
Hourly, best-effort (SCOPING section 5.1). Per site: fetches each
configured obs source over a short live window, runs \code{qc_run()}/
\code{fill_run()} over that window, applies current calibrations
(\code{correct_apply()}, \code{target = "record"} for observations and
\code{target = "forecast"} for the forecast head), archives current forecast
issuances (\code{archive_forecasts()}), and advances the live watermark
(\code{"observations"}/\code{"live"}).
}
\details{
GHCNh is never fetched here: its ~1-week lag (Plan 06 cadence metadata)
makes it unsuitable for a live head; it participates in
\code{met_sync_daily()}'s history products instead. A dead acquisition source
marks that site's status \code{"degraded"} (not an error) and the run
continues with the other configured sources; other sites are unaffected
(\code{for_each_site(on_error = "isolate")}).

Multi-site, incremental (the live watermark), and idempotent: re-running
with the same inputs and clock does not duplicate observation rows or
double-advance the watermark (Plan 03's supersede/dedup policies).
}
\examples{
\dontrun{
met_sync_live(site, config = my_pipeline_config)
}
}
\seealso{
Other pipeline: 
\code{\link{met_backfill}()},
\code{\link{met_refit}()},
\code{\link{met_sync_daily}()}
}
\concept{pipeline}
