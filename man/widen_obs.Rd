% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema-obs.R
\name{widen_obs}
\alias{widen_obs}
\title{Pivot a canonical observation table to wide (Open-Meteo-named) form}
\usage{
widen_obs(obs, variables = NULL)
}
\arguments{
\item{obs}{A canonical observation tibble (see \code{new_obs()}).}

\item{variables}{Character vector of variable names to include as columns.
Defaults to every variable currently in the dictionary.}
}
\value{
A wide tibble with columns \code{site_id}, \code{datetime_utc}, and one
column per requested variable.
}
\description{
One row per \verb{(site_id, datetime_utc)}; columns are the requested
\code{variables} (default: the full dictionary). A variable absent from \code{obs}
still produces an all-\code{NA} column, keeping the wide §3.1 shape stable
regardless of coverage. The time column stays named \code{datetime_utc}
internally; Plan 15's emitter does the \code{time} rename at the outer
boundary.
}
\examples{
widen_obs(new_obs(tibble::tibble(
  site_id = "test",
  datetime_utc = as.POSIXct("2026-01-01", tz = "UTC"),
  variable = "temperature_2m",
  value = 20,
  source = "test_src",
  method = "measured",
  qc_flag = "ok"
)))
}
\seealso{
Other schema: 
\code{\link{narrow_obs}()}
}
\concept{schema}
