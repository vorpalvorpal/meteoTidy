% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/station-resolve.R
\name{nearest_stations}
\alias{nearest_stations}
\title{Find the nearest stations in a catalogue to a point}
\usage{
nearest_stations(site_lat, site_lon, catalogue, n = 1)
}
\arguments{
\item{site_lat, site_lon}{Single numeric degrees, the reference point.}

\item{catalogue}{A data frame with (at least) \code{station_id}, \code{latitude},
\code{longitude} columns, and optionally \code{identity} (see Details).}

\item{n}{Integer, how many nearest stations to return. If \code{catalogue} (after
dedup) has fewer than \code{n} rows, all of them are returned.}
}
\value{
A data frame with the original \code{catalogue} columns plus
\code{distance_km}, the \code{n} (or fewer) nearest rows ordered ascending by
\code{distance_km}.
}
\description{
Computes the great-circle (haversine) distance from \verb{(site_lat, site_lon)}
to every row of \code{catalogue}, deduplicates by physical station identity
(see Details), and returns the \code{n} nearest, ascending by distance.
}
\details{
If \code{catalogue} has an \code{identity} column, rows sharing the same \code{identity}
value are treated as the same physical station (e.g. a station served via
both a real-time transport and GHCNh, under two different \code{station_id}s)
and collapsed to a single row — the nearest of the group — before the \code{n}
nearest are selected. This is what the gap-fill donor ladder (Plan 10)
relies on to avoid double-counting a station reached by two routes. When
\code{catalogue} has no \code{identity} column, no deduplication happens (every row
is its own station).
}
\examples{
cat <- data.frame(
  station_id = c("a", "b"),
  latitude = c(-34.76, -35.50),
  longitude = c(148.21, 149.00)
)
nearest_stations(-34.75, 148.20, cat, n = 1)
}
\concept{station-resolution}
