% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met-table.R
\name{new_met_table}
\alias{new_met_table}
\title{Construct a \code{met_table}, the meteoHazard classed-tibble interface}
\usage{
new_met_table(x, provenance, keys, versions)
}
\arguments{
\item{x}{The underlying wide tibble (SCOPING section 3.1 columns): a
\code{time} column plus one column per variable.}

\item{provenance}{A tibble with columns \code{variable}, \code{tier},
\code{train_overlap}, \code{source} -- one row per value column of \code{x}. Every
value column (\code{setdiff(names(x), "time")}) must be covered.}

\item{keys}{A list with at least \code{site_id}; optionally \code{from}/\code{to} (the
window the table was read over).}

\item{versions}{A list with \code{schema_version} and
\code{calibration_manifest_version}.}
}
\value{
A \code{met_table}.
}
\description{
\code{met_table} is an S3 subclass of \code{tbl_df} (SCOPING section 3.2, the
sf/tsibble pattern rather than an opaque S7 wrapper) carrying per-variable
provenance, site/window keys, and schema/calibration-manifest versions as
attributes, plus a per-column content hash so staleness is detectable
(see \code{\link[=met_validate_boundary]{met_validate_boundary()}}). \code{dplyr} verbs keep working on it
untouched (see \code{R/met-table-dplyr.R}): column-preserving operations keep
the class; operations that invalidate the metadata downgrade visibly to a
plain tibble with a warning.
}
\examples{
wide <- tibble::tibble(
  time = as.POSIXct("2026-01-01", tz = "UTC"),
  temperature_2m = 20
)
prov <- tibble::tibble(variable = "temperature_2m", tier = "raw",
                       train_overlap = 0, source = "openmeteo")
new_met_table(wide, provenance = prov, keys = list(site_id = "test"),
             versions = list(schema_version = "1.0.0",
                             calibration_manifest_version = 0L))
}
\seealso{
Other met-table: 
\code{\link{met_assert_single_tier}()},
\code{\link{met_ingest}()},
\code{\link{met_provenance}()},
\code{\link{met_validate_boundary}()},
\code{\link{met_wide}()}
}
\concept{met-table}
