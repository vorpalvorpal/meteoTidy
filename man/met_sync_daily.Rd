% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met-sync-daily.R
\name{met_sync_daily}
\alias{met_sync_daily}
\title{Sync forecasts and extend curated history products (daily)}
\usage{
met_sync_daily(sites, now = .now(), config)
}
\arguments{
\item{sites}{A \code{met_site} or \code{met_sites} collection.}

\item{now}{Injectable current time; see \code{.now()}.}

\item{config}{A pipeline configuration list (see \verb{plans/14-*}/
\code{tests/testthat/helper-pipeline.R}'s \code{pipeline_config()}): at least
\code{store_root}, \code{obs_sources}, \code{forecast_sources}.}
}
\value{
A tibble with columns \code{site_id}, \code{status}, \code{message}.
}
\description{
Per site (SCOPING section 9): archives every configured forecast source's
current issuances (\code{archive_forecasts()}, including seasonal products
when configured); re-fetches each configured obs source over its
\strong{refetch window} (\code{config$refetch_windows[[source]]}, e.g. SILO's 30
days, so upstream revisions supersede rather than duplicate -- Plan
03/06); runs \code{qc_run()}/\code{fill_run()}; extends \code{history_hourly}/
\code{history_daily} (\code{build_history_hourly()}/\code{build_history_daily()}, Plan
10); and advances each obs source's daily watermark.
}
\details{
Multi-site, incremental, and idempotent, mirroring \code{met_sync_live()}. A
dead obs source degrades that site's status to \code{"degraded"} rather than
propagating; other sites are unaffected.
}
\examples{
\dontrun{
met_sync_daily(site, config = my_pipeline_config)
}
}
\seealso{
Other pipeline: 
\code{\link{met_backfill}()},
\code{\link{met_refit}()},
\code{\link{met_sync_live}()}
}
\concept{pipeline}
