% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/units.R
\name{to_canonical}
\alias{to_canonical}
\title{Convert a value into a variable's canonical unit}
\usage{
to_canonical(x, from, variable)
}
\arguments{
\item{x}{A numeric or \code{units}-classed vector.}

\item{from}{A single string giving the unit \code{x} is expressed in (ignored,
with a warning on disagreement, when \code{x} already carries units).}

\item{variable}{A single dictionary variable name; determines the target
canonical unit.}
}
\value{
A \code{units}-classed vector in the canonical unit for \code{variable}.
}
\description{
Converts \code{x}, expressed in unit \code{from}, into the canonical unit for
\code{variable} (looked up in the variable dictionary). If \code{x} already carries
\code{units} (i.e. is a \code{units}-classed vector), the carried unit is honoured
and \code{from} is ignored — except that a disagreement between the carried
unit and \code{from} raises a warning (class \code{"units_conflict"}); it never
errors, since the carried unit is trusted over the caller's claim.
}
\details{
This is the choke point for the "km/h footgun" (SCOPING §3.1): the
canonical wind-speed unit is \code{m/s}, so a value supplied as \code{km/h} is always
converted, never silently passed through.
}
\examples{
to_canonical(10, "km/h", "wind_speed_10m")
}
\seealso{
Other units: 
\code{\link{canonical_unit}()}
}
\concept{units}
