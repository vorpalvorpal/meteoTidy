% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dict.R
\name{met_register_variable}
\alias{met_register_variable}
\title{Register a variable in the dictionary}
\usage{
met_register_variable(
  variable,
  unit,
  min = NA_real_,
  max = NA_real_,
  statistical_class,
  measurability_class,
  circular_period = NA_real_,
  description,
  overwrite = FALSE
)
}
\arguments{
\item{variable}{Single string, the canonical variable name.}

\item{unit}{Single string, a \code{units}-package-parseable unit.}

\item{min, max}{Single doubles (canonical unit), or \code{NA} if unbounded on
that side. Must satisfy \code{min <= max} when both are given.}

\item{statistical_class}{Single string, one of \link{STAT_CLASS_LEVELS}.}

\item{measurability_class}{Single string, one of \link{MEASURABILITY_LEVELS}.}

\item{circular_period}{Single double or \code{NA} (default). Use \code{360} for
angular/direction variables.}

\item{description}{Single string, a one-line description.}

\item{overwrite}{Logical, default \code{FALSE}. Must be \code{TRUE} to redefine an
existing variable (built-in or previously registered).}
}
\value{
The updated dictionary tibble, invisibly.
}
\description{
Adds (or, with \code{overwrite = TRUE}, replaces) a row in the variable
dictionary. Attempting to silently redefine a built-in without
\code{overwrite = TRUE} aborts.
}
\examples{
met_register_variable(
  variable = "leaf_wetness", unit = "1", min = 0, max = 1,
  statistical_class = "bounded", measurability_class = "site_measurable",
  description = "Fraction of time the leaf surface is wet.",
  overwrite = TRUE
)
}
\seealso{
Other dictionary: 
\code{\link{met_variable}()},
\code{\link{met_variables}()}
}
\concept{dictionary}
