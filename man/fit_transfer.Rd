% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transfer.R
\name{fit_transfer}
\alias{fit_transfer}
\title{Fit a transfer (bias-correction) transform between two realised series}
\usage{
fit_transfer(
  source,
  target,
  method = c("mean_bias", "qmap"),
  by = NULL,
  treatment = NULL
)
}
\arguments{
\item{source}{A single-variable canonical long obs tibble (the series
being corrected, e.g. a donor station).}

\item{target}{A single-variable canonical long obs tibble (the series
being corrected towards, e.g. the site's own record).}

\item{method}{Either \code{"mean_bias"} (a constant additive offset) or
\code{"qmap"} (empirical quantile mapping, correcting the full distribution
shape, not just the mean).}

\item{by}{Optional grouping column name(s) in both series (e.g. an hour-
of-day block) to fit the transform conditionally. \code{NULL} (default) fits
one unconditional transform over the whole overlap.}

\item{treatment}{Optional name of the per-variable statistical space the
transform should be fit in (see \code{R/fill-treatments.R}); \code{NULL} (default)
fits directly on \code{value}. Reserved for callers that have already
converted \code{source}/\code{target} into the right space themselves.}
}
\value{
A plain list of fitted parameters (never an \code{.rds} model object):
\code{method}, plus \code{offset} (mean_bias) or \code{probs}/\code{source_quantiles}/
\code{target_quantiles} (qmap).
}
\description{
Fits a transform that maps \code{source_series} onto \code{target_series} over their
shared/overlapping window (matched on \code{datetime_utc}). Both arguments are
single-variable canonical long obs tibbles. This is the shared primitive
behind gap-fill's donor bias-correction and Plan 12's forecast correction
tiers; \strong{it assumes both series are realised observations, over the same
physical window, with no forecast lead time involved} -- there is no
skill-decay or shrinkage concept here (Plan 12 adds that as a wrapper).
}
\examples{
target <- data.frame(
  site_id = "test",
  datetime_utc = seq(as.POSIXct("2026-01-01", tz = "UTC"), by = "hour", length.out = 10),
  variable = "temperature_2m", value = seq(10, 19),
  source = "site", method = "measured", qc_flag = "ok"
)
source_series <- target
source_series$value <- target$value + 2
tr <- fit_transfer(source_series, target, method = "mean_bias")
apply_transfer(tr, source_series)
}
\seealso{
Other transfer: 
\code{\link{apply_transfer}()}
}
\concept{transfer}
