% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transfer.R
\name{apply_transfer}
\alias{apply_transfer}
\title{Apply a fitted transfer to a source series}
\usage{
apply_transfer(transfer, source_series)
}
\arguments{
\item{transfer}{A transform fitted by \code{\link[=fit_transfer]{fit_transfer()}}.}

\item{source_series}{A single-variable canonical long obs tibble to
correct.}
}
\value{
\code{source_series} with \code{value} replaced by the corrected values.
}
\description{
Applies a transform fitted by \code{\link[=fit_transfer]{fit_transfer()}} to \code{source_series} (which
may be a different slice, or a different but comparable series, than the
one the transform was fit on). Returns \code{source_series} with \code{value}
corrected; every other column is unchanged.
}
\details{
\strong{Frozen contract:} this function has no \code{lead}/\code{lead_time}/\code{weight}/
\code{shrink}/\code{shrinkage} argument, and never will -- the transfer engine is
skill-decay-free by design (SCOPING section 6). Applying the same fitted
transfer to an early or a late row of the same source series yields an
identical correction; Plan 12 adds lead-dependent shrinkage as a wrapper
around this primitive, never inside it.
}
\examples{
target <- data.frame(
  site_id = "test",
  datetime_utc = seq(as.POSIXct("2026-01-01", tz = "UTC"), by = "hour", length.out = 10),
  variable = "temperature_2m", value = seq(10, 19),
  source = "site", method = "measured", qc_flag = "ok"
)
source_series <- target
source_series$value <- target$value + 2
tr <- fit_transfer(source_series, target, method = "mean_bias")
apply_transfer(tr, source_series)
}
\seealso{
Other transfer: 
\code{\link{fit_transfer}()}
}
\concept{transfer}
