% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_run}
\alias{fill_run}
\title{Run the gap-fill engine over a site's observation window}
\usage{
fill_run(
  store_root,
  site,
  variables = NULL,
  now = .now(),
  donors = NULL,
  model = NULL
)
}
\arguments{
\item{store_root}{Root directory of the store.}

\item{site}{A \code{met_site} object.}

\item{variables}{Character vector of variables to fill; \code{NULL} (default)
means every variable present in the window.}

\item{now}{Injectable current time; see \code{.now()}.}

\item{donors}{A named list of single-variable long donor obs tibbles (see
\code{fill_medium()}), or \code{NULL} (default) if none are available yet (the
micro tier and, if \code{model} is supplied, the macro tier still apply).}

\item{model}{A single long obs tibble (a model series, e.g.
\code{source = "openmeteo"}) covering the window, for the macro tier and for
\code{model_only} variables; \code{NULL} (default) if none is available.}
}
\value{
Invisibly, a list \code{(n_filled)} summarising the run.
}
\description{
Reads the fill watermark for \code{site} (a store watermark keyed by \code{source = "fill"}), applies a small look-back (\code{.fill_lookback()}) so a better donor
arriving after an earlier fill can still supersede it, reads the
resulting window's observations, detects gaps per variable (\code{NA}/
\code{"missing"}-flagged \code{value} rows), fills them via \code{fill_tier()}, writes
back only the rows whose value/method actually changed via
\code{store_write_obs(mode = "supersede")} (so an earlier fill stays
retrievable via \code{include_superseded = TRUE} when a later run replaces it
with a better one), and advances the watermark to \code{now}.
}
\details{
Incremental: only the window from \code{watermark - lookback} to \code{now} is
scanned. Idempotent: re-running over the same window with the same inputs
reproduces identical filled values and does not create duplicate rows
(the store's supersede path already de-duplicates identical
value/qc_flag/method rows -- see \code{store_write_obs()}, Plan 03).
}
\examples{
\dontrun{
root <- withr::local_tempdir()
site <- met_site(
  site_id = "example",
  latitude = units::set_units(-34.75, "degree"),
  longitude = units::set_units(148.20, "degree"),
  elevation = units::set_units(220, "m"),
  timezone = "Australia/Sydney",
  instruments = list(),
  sources = list(),
  store_root = root
)
fill_run(root, site)
}
}
\concept{fill}
