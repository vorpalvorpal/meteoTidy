% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-api.R
\name{met_history}
\alias{met_history}
\title{The curated history record (\code{history_daily}/\code{history_hourly})}
\usage{
met_history(
  site,
  resolution = c("daily", "hourly"),
  variables = NULL,
  from = NULL,
  to = NULL,
  as_of = NULL
)
}
\arguments{
\item{site}{A \code{\link[=met_site]{met_site()}} or \code{\link[=met_sites]{met_sites()}}.}

\item{resolution}{Either \code{"daily"} (default) or \code{"hourly"}.}

\item{variables}{Optional character vector to restrict which variables
are returned; \code{NULL} (default) returns every variable present.}

\item{from, to}{Optional UTC POSIXct bounds.}

\item{as_of}{Optional UTC POSIXct point-in-time read. Accepted for
interface symmetry with \code{\link[=met_record]{met_record()}}; the underlying curated-product
builders (Plan 10) have no revision concept yet, so this currently has
no effect (a documented gap, not silently ignored data corruption).}
}
\value{
A canonical long observation tibble (see \code{new_obs()}).
}
\description{
Returns the QC'd, gap-filled, curated observation history at daily or
hourly resolution (SCOPING §4): SILO-based \code{history_daily}, or hourly
aggregates for \code{history_hourly}, with provenance recording which leg
(SILO vs. the site's own AWS) served each value.
}
\examples{
\dontrun{
met_history(site, resolution = "daily")
}
}
\seealso{
Other read-api: 
\code{\link{met_connect}()},
\code{\link{met_forecast_archive}()},
\code{\link{met_record}()},
\code{\link{met_verification}()}
}
\concept{read-api}
