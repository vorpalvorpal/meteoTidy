% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source-file.R
\name{source_file}
\alias{source_file}
\title{A local logger-file observation adapter}
\usage{
source_file(
  source_id,
  glob,
  mapping,
  provides = NULL,
  cadence = "daily",
  delim = ",",
  has_header = TRUE,
  na = "NA",
  skip = 0
)
}
\arguments{
\item{source_id}{Single string, stamped into the \code{source} column of every
returned row.}

\item{glob}{Single string, a \code{Sys.glob()} pattern matching one or more
files to read.}

\item{mapping}{A \code{\link[=met_mapping]{met_mapping()}} (with \code{format = "csv"}) describing how to
turn each parsed file into canonical rows.}

\item{provides}{Character vector of variables this adapter can return.
Defaults to the variable names declared in \code{mapping}.}

\item{cadence}{Single string, a scheduling hint (default \code{"daily"}).}

\item{delim}{Single string, the field delimiter (default \code{","}).}

\item{has_header}{Logical, does each file have a header row naming
columns matching \code{mapping}'s \code{column} entries (default \code{TRUE})?}

\item{na}{Character vector of strings to treat as \code{NA} (default \code{"NA"}).}

\item{skip}{Integer, number of preamble lines to skip before the header
(default \code{0}).}
}
\value{
A \code{source_file} (\code{met_adapter} subclass) S7 object.
}
\description{
\code{source_file()} builds a \code{\link[=met_adapter]{met_adapter()}} that reads one or more local
CSV/TSV logger exports matched by \code{glob}, concatenates them in
deterministic time order, and maps them to canonical observations via
\code{\link[=apply_mapping]{apply_mapping()}}. Handles the common messiness of logger exports minimally
(configurable delimiter, header, \code{na} strings, and a \code{skip} count for
preamble lines); anything beyond that is a user-written adapter.
}
\examples{
adapter <- source_file(
  "logger", glob = tempfile(fileext = ".csv"),
  met_mapping(
    format = "csv",
    time = list(column = "timestamp", tz = "UTC"),
    variables = list(
      list(variable = "temperature_2m", column = "temp_c", unit = "degC")
    )
  )
)
}
\seealso{
Other adapter: 
\code{\link{adapters_for_site}()},
\code{\link{apply_mapping}()},
\code{\link{fetch}()},
\code{\link{fetch_forecast}()},
\code{\link{met_adapter}()},
\code{\link{met_mapping}()},
\code{\link{resolve_station}()},
\code{\link{source_rest}()}
}
\concept{adapter}
