% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met-ingest.R
\name{met_ingest}
\alias{met_ingest}
\title{The meteoHazard dual-accept boundary validator}
\usage{
met_ingest(x)
}
\arguments{
\item{x}{A \code{met_table} or a plain tibble.}
}
\value{
A \code{met_table}.
}
\description{
A consumer (meteoHazard) calls \code{met_ingest()} once at its boundary
(SCOPING section 3.2):
}
\details{
\itemize{
\item If \code{x} is already a \code{\link[=new_met_table]{new_met_table()}}: \code{\link[=met_validate_boundary]{met_validate_boundary()}} (the hash
re-check) runs once, and the (possibly per-column-downgraded)
provenance is trusted thereafter.
\item If \code{x} is a plain tibble: the section 3.1 schema is validated on entry
(a \code{time} column must be present) and, if valid, every value column's
provenance is marked \code{tier = "unverified"} -- nothing is known about
how a plain tibble was produced, so its metadata cannot be trusted.
}
}
\examples{
plain <- tibble::tibble(time = as.POSIXct("2026-01-01", tz = "UTC"),
                        temperature_2m = 20)
met_ingest(plain)
}
\seealso{
Other met-table: 
\code{\link{met_assert_single_tier}()},
\code{\link{met_provenance}()},
\code{\link{met_validate_boundary}()},
\code{\link{met_wide}()},
\code{\link{new_met_table}()}
}
\concept{met-table}
