% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema-obs.R
\name{narrow_obs}
\alias{narrow_obs}
\title{Pivot a wide observation table back to canonical long form}
\usage{
narrow_obs(wide, source = NA_character_, method = "measured", qc_flag = "ok")
}
\arguments{
\item{wide}{A wide tibble as produced by \code{\link[=widen_obs]{widen_obs()}}.}

\item{source, method, qc_flag}{Defaults used to fill the long-form metadata
columns that the wide table does not carry.}
}
\value{
A canonical long-format observation tibble (not re-validated by
\code{new_obs()}, since the fill-in metadata may not reflect the truth).
}
\description{
The inverse of \code{\link[=widen_obs]{widen_obs()}}. \code{widen_obs(obs) |> narrow_obs()} is the
identity on \verb{(site_id, datetime_utc, variable, value)} (round-trip
contract); other obs columns (\code{source}, \code{method}, \code{qc_flag}) are not
recoverable from the wide form and are filled with defaults
(\code{source}/\code{method}/\code{qc_flag} arguments) since the wide table does not
carry them.
}
\examples{
narrow_obs(widen_obs(new_obs(tibble::tibble(
  site_id = "test",
  datetime_utc = as.POSIXct("2026-01-01", tz = "UTC"),
  variable = "temperature_2m",
  value = 20,
  source = "test_src",
  method = "measured",
  qc_flag = "ok"
))))
}
\seealso{
Other schema: 
\code{\link{widen_obs}()}
}
\concept{schema}
