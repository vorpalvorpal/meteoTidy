% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{met_connect}
\alias{met_connect}
\title{Connect to a site's store for ad-hoc SQL/dplyr access}
\usage{
met_connect(site, backend = c("duckdb", "arrow"))
}
\arguments{
\item{site}{A \code{\link[=met_site]{met_site()}} object.}

\item{backend}{Either \code{"duckdb"} (a \code{DBI} connection with \code{observations}/
\code{forecasts}/\code{forecast_aux} views registered for whichever datasets
exist) or \code{"arrow"} (a named list of open arrow \code{Dataset} objects).
Default \code{"duckdb"}.}
}
\value{
For \code{backend = "duckdb"}, a \code{DBI} connection. For \code{backend = "arrow"}, a named list of arrow \code{Dataset} objects.
}
\description{
\strong{Experimental.} Thin wrapper around the internal \code{store_connect()}
(Plan 03) for a single \code{\link[=met_site]{met_site()}}: returns a live connection over the
\emph{same} Parquet tree the stable tibble read API (\code{\link[=met_record]{met_record()}},
\code{\link[=met_history]{met_history()}}, ...) reads, but exposes the raw physical schema
(including bookkeeping columns like \code{superseded}/\code{ingested_at}) rather
than the curated tibble contract. Only the tibble read functions are a
stability promise (SCOPING §11) -- this function's exact schema may
change without a deprecation cycle.
}
\examples{
\dontrun{
con <- met_connect(site, backend = "duckdb")
dplyr::tbl(con, "observations")
}
}
\seealso{
Other read-api: 
\code{\link{met_forecast_archive}()},
\code{\link{met_history}()},
\code{\link{met_record}()},
\code{\link{met_verification}()}
}
\concept{read-api}
