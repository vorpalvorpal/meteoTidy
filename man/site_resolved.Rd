% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site.R
\name{site_resolved}
\alias{site_resolved}
\alias{site_set_resolved}
\title{Read or set a site's resolved external-ID cache}
\usage{
site_resolved(site, path = NULL)

site_set_resolved(site, path, value)
}
\arguments{
\item{site}{A \code{\link[=met_site]{met_site()}} object.}

\item{path}{Character vector of nested list names, e.g.
\code{c("ghcnh", "station_id")}. \code{NULL} (the default for \code{site_resolved()})
returns the whole \code{resolved} list.}

\item{value}{The value to set at \code{path}.}
}
\value{
\code{site_resolved()} returns the value at \code{path} (or the whole
\code{resolved} list if \code{path = NULL}). \code{site_set_resolved()} returns a new
\code{met_site} object with \code{resolved} updated; the original is unchanged.
}
\description{
The \code{resolved} slot is a passive cache of external station/grid
identifiers (BOM geohash/AAC/product, nearest GHCNh station, SILO grid
cell) populated by adapters (Plans 05-07). This plan only provides the
accessors: \code{site_resolved()} reads (optionally at a \code{path} into the nested
list), and \code{site_set_resolved()} returns a \strong{new} site with the value set
— \code{met_site} objects are never mutated in place (house style: functional
by default).
}
\examples{
site <- met_site(
  site_id = "example",
  latitude = units::set_units(-34.75, "degree"),
  longitude = units::set_units(148.20, "degree"),
  elevation = units::set_units(220, "m"),
  timezone = "Australia/Sydney",
  instruments = list(),
  sources = list(),
  store_root = tempfile()
)
updated <- site_set_resolved(site, c("ghcnh", "station_id"), "ASN00072150")
site_resolved(updated, c("ghcnh", "station_id"))
}
\seealso{
Other site: 
\code{\link{as_met_sites}()},
\code{\link{met_instrument}()},
\code{\link{met_site}()},
\code{\link{met_sites}()},
\code{\link{read_sites_yaml}()},
\code{\link{site_id}()},
\code{\link{site_ids}()},
\code{\link{write_sites_yaml}()}
}
\concept{site}
